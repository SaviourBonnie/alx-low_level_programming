#include "main.h"

/**
 * main - Test the _puts function.
 *
 * Return: Always 0.
 */
int main(void)
{
	char *str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
	_puts(str);
	return (0);
}
